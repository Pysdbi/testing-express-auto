describe('Cars CRUD api', () => {
  it('Get all cars', () => {
    expect(1).toEqual(1)
  })
  it('Get one car', () => {
    expect(1).toEqual(1)
  })
  it('Create car', () => {
    expect(1).toEqual(1)
  })
  it('Update car', () => {
    expect(1).toEqual(1)
  })
  it('Delete car', () => {
    expect(1).toEqual(1)
  })
})
