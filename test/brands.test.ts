// Задача: "Написать несколько юниттестов с использованием jest или mocha для проверки функционала CRUD из п.5,
// желательно не задействуя REST API. Уровень покрытия выбрать самостоятельно."
//
//
// *** Тестирование с поднятой БД уже считается интеграционным.

describe('Brands CRUD api', () => {
  it('Get all brands', async () => {
   expect(1).toEqual(1)
  })

  it('Create brand', () => {
    expect(1).toEqual(1)
  })
  it('Delete brand', () => {
    expect(1).toEqual(1)
  })
})
