describe('Brands CRUD api', () => {
  it('Get all brands', () => {
    expect(1).toEqual(1)
  })
  it('Get one brand', () => {
    expect(1).toEqual(1)
  })
  it('Create brand', () => {
    expect(1).toEqual(1)
  })
  it('Update brand', () => {
    expect(1).toEqual(1)
  })
  it('Delete brand', () => {
    expect(1).toEqual(1)
  })
})
